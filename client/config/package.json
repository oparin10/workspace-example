{
  "private": true,
  "name": "@buffme/config",
  "description": "Shared config files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}
